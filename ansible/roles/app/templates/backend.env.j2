# Variables de entorno para CKJ Backend
# Generado por Ansible - no editar manualmente

PORT={{ backend_port }}
JWT_SECRET={{ jwt_secret }}
DB_PATH={{ db_path }}
UPLOADS_DIR={{ uploads_dir }}
NODE_ENV=production
